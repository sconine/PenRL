# Intel macOS: PyTorch wheels stop at torch 2.2.x; Stable-Baselines3 >=2.5 needs torch>=2.3 (Apple Silicon only).
# Use SB3 2.4.x + numpy 1.x. Requires Python 3.10–3.12 (no cp313 wheels for torch on Intel macOS).
numpy>=1.24,<2
mujoco>=3.1.0
gymnasium>=0.29.1,<1.1.0
tensorboard>=2.14.0
torch==2.2.2
stable-baselines3==2.4.1
